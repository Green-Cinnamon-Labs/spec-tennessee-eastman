[project]
name = "tep-analysis"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8",
    "pandas>=2.0",
]

[project.scripts]
plot = "tep_analysis.plot:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
